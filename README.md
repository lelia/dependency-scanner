# dependency-scanner

A tool to scan dependency manifest files and report known vulnerabilities.

## Current limitations

- Only supports `package-lock.json`
- Only queries [OSV](https://osv.dev)
- Outputs results to console, not JSON
