/**
 * API Client for GHSA (GitHub Security Advisory) database.
 * 
 * Optional: Fine-grained PAT (Personal Access Token) with security advisories scope.
 * Usage: Export as `GITHUB_TOKEN` environment variable or pass as `--github-token` flag.
 * 
 * Ref: https://docs.github.com/en/rest/security-advisories/global-advisories
 */
